
const APP_URL = "http://localhost:8000";

   
const E = React.createElement;
/*
function func_with_arg(func, arg) {
    params = [arg].concat(arguments);
    return func.apply(params)
}
const DIV = func_with_arg(React.createElement, "div");
const LABEL = React.createElement.bind("label");
const INPUT = React.createElement.bind("input");
*/

